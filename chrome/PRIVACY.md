# سياسة الخصوصية — Nasaq

**آخر تحديث:** مايو 2026

## ملخّص

Nasaq لا تجمع، لا تخزّن، ولا ترسل أي بيانات شخصية إلى أي خادم.

كل ما تفعله الإضافة يحدث **محليًا على جهازك**.

---

## ما تخزّنه الإضافة

تستخدم Nasaq `chrome.storage.local` لحفظ:

- **الإعدادات العامة** (تفعيل، ثيم، تبويبة افتراضية)
- **قائمة المواقع المُفعَّل عليها**
- **قواعد CSS التي أنشأتها** لكل موقع

هذه البيانات تبقى **على جهازك فقط**، ولا تُزامن أو تُرسل خارجيًا.

## ما **لا** تفعله الإضافة

- ❌ لا تجمع أي بيانات تعريفية
- ❌ لا تتتبّع تصفّحك أو نشاطك
- ❌ لا ترسل بيانات إلى أي خادم
- ❌ لا تستخدم Google Analytics أو أي تتبّع
- ❌ لا تحمّل خطوطًا من CDN — كل الخطوط مدمجة محليًا
- ❌ لا تحوي إعلانات

## الصلاحيات

| الصلاحية | الغرض |
|---|---|
| `storage` | حفظ تفضيلاتك وأنماطك محليًا |
| `sidePanel` | عرض اللوحة الجانبية |
| `scripting` | حقن CSS وأداة الاختيار في الصفحات |
| `activeTab` | التعرّف على التبويب الحالي عند فتح اللوحة |
| `tabs` | قراءة hostname الموقع للتحديد أي قواعد تنطبق |
| `contextMenus` | إضافة خيارات للقائمة اليمنى |
| `host_permissions: <all_urls>` | تطبيق CSS تلقائيًا على المواقع التي عدّلتها |

كل هذه الصلاحيات تُستخدم **فقط** للوظائف الموصوفة، ولا تُستخدم لجمع بيانات.

## مشاركة البيانات

Nasaq **لا تشارك** أي بيانات مع أي طرف ثالث، لأنها لا تجمع بيانات أصلًا.

## التصدير والاستيراد

عند استخدام ميزة **تصدير الإعدادات**، يُنشأ ملف JSON يحوي إعداداتك وأنماطك. هذا الملف يُحفظ على جهازك مباشرة ولا يُرسل لأي مكان. يمكنك حذفه متى شئت.

## مزامنة Google Drive (ميزة قادمة)

إذا فعّلت مزامنة Google Drive في إصدار مستقبلي، فسيُرفع ملف JSON واحد إلى Drive **الخاص بك**. هذا الاتصال يستخدم OAuth الرسمي من Google، ولا يمر عبر أي خادم تابع لنا. ستحتاج موافقتك الصريحة لتفعيل هذه الميزة.

## التواصل

للاستفسارات حول الخصوصية، افتح Issue على مستودع المشروع.

## التغييرات على هذه السياسة

أي تغيير على سياسة الخصوصية سيُنشَر هنا مع رفع رقم الإصدار.
